int birthdayCakeCandles(int candles_count, int* candles) {
    int max=0;
    int i;
    int c=0;
    for(i=0;i<candles_count;i++)
    {
        if(candles[i]>max)
    {
        max=candles[i];
        
    }
    }
    for(i=0;i<candles_count;i++)
    {
        if(candles[i]==max)
        {
            c++;
        }
    }
return c;
}
